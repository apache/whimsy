pending = Pending.get
pending['initials'] = @initials
pending['agenda'] = @agenda

approved = pending['approved']

if @request == 'approve'
  approved << @attach
else
  approved.delete @attach
end

Pending.put(pending)

_! pending
